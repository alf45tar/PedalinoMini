plugins {
  id "org.sonarqube" version "2.8"
}

sonarqube {
  properties {
    property "sonar.projectKey", "alf45tar_PedalinoMini"
    property "sonar.organization", "alf45tar"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}